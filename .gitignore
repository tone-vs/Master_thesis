# RStudio / R session files
.Rproj.user
.Rhistory
.RData
.Ruserdata

# R environment files — contain API keys, never commit
.Renviron
.Renviron.sh

# macOS system files
.DS_Store

# Intermediate API download cache — large, regenerable, not needed in repo
data/raw/chunks/

# Raw data files — too large for git; download instructions in README
# data/raw/
# data/semiconductor/

# Processed outputs — regenerable from scripts
# (commented out: keep processed files in repo so collaborators can run the
#  analysis Rmd without re-running the API download scripts)
# data/processed/
.positai
.claude/
